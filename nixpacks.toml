[phases.setup]
nodejs_version = "18.17.0"
aptPkgs = ["openssl", "postgresql-client"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
    "npx prisma generate",
    "npm run build"
]

[start]
cmd = "npm run start"

